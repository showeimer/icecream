const mongoose = require('mongoose');
const Runner = mongoose.model('runners');

module.exports = app => {

  // CREATE ENDPOINTS ============================================
  // READ ENDPOINTS ================================================
  // UPDATE ENDPOINTS ================================================
  // DELETE ENDPOINTS ============================================

}
